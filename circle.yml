machine:
  php:
    version: 5.6.3
  phpunit:
    version: 4.3.4

dependencies:
  pre:
    - npm install --global shipit-cli
    - npm install --global shipit-deploy

deployment:
  production:
    branch: master
    commands:
      - shipit staging deploy
