dependencies:
  pre:
    - npm install --global shipit-cli
    - npm install --global shipit-deploy

test:
  post:
    - true

deployment:
  production:
    branch: master
    commands:
      - shipit staging deploy
